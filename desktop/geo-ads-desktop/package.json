{
  "name": "geo-ads-desktop",
  "version": "0.1.0",
  "main": "main.js",
  "private": true,
  "scripts": {
    "build:ui": "cross-env REACT_APP_BACKEND_BASE_URL=http://127.0.0.1:8000 npm --prefix ../../frontend/geo-ads-frontend run build",
    "desktop": "npm run build:ui && electron ."
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "cross-env": "^7.0.3",
    "tree-kill": "^1.2.2"
  }
}
